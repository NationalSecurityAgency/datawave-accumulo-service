name: Tests

on:
  push:
    paths-ignore: ['*.md', 'CODEOWNERS', 'LICENSE']
    branches:
    - 'main'
    - 'release/*'
  pull_request:
    paths-ignore: ['*.md', 'CODEOWNERS', 'LICENSE']

jobs:
  call-reusable-workflow:
    uses: nationalsecurityagency/datawave/.github/microservice-workflows/maven-tests-reusable.yaml@add-microservice-workflows